## Prompt

add Deno tests in `src` using built in Deno Testing features, filenames same as the code file (but with `.test.ts` suffix) and cover all current features

## Result

All 47 tests pass. Here's a summary of what was done:

**Files created:**

- `src/api/kv.test.ts` — 8 tests covering `kvGetTask` (null for unknown, retrieval after save), `kvSaveTask` (persist, overwrite), and `kvListTasks` (deleted filtering, `owner_id`/`project_id` filters)
- `src/api/handlers.test.ts` — 26 tests covering all five handlers: list (filters, deleted visibility), get (found/not found/deleted), create (success, trim, optional fields, missing/blank title, bad JSON), update (field patching, null resets, timestamp bump, 404/400 cases), delete (soft-delete, idempotent 404)
- `src/api/router.test.ts` — 13 tests covering correct routing to each handler, method-not-allowed (405) on both collection and item routes, `/health`, and unknown-path 404

**Files modified:**

- `src/api/kv.ts` — exported `setKv()` so tests can inject an in-memory `Deno.Kv` instance
- `deno.json` — added `--unstable-kv` to the test command and `@std/assert@^1.0.19` to imports
